# py学习
## 字面量-在代码中，被写下来的固定的值
###  1.字符串
     文本，有英文，中文和各种符号。  跟c++无区别，整数和浮点数，字符串用双引,并且是str,浮点数用float，布尔类型。
###  2.多了一个空值跟c++的null差不多，表示None


### 3.数据容器--存储多项数据的容器————列表，元组，集合，字典

### py的注释是#


## 变量 存储单个数据的容器---动态类型语言，一个变量可以存储不同类型的语言

### 1.定义变量的时候不需要int 这些，编辑器会自动识别

### 2.print 可以输出多个数据 拼接在一行

### 3.一次性可以定义多个变量

## 标识符--在代码中为变量，函数，类所起的名字 -- c++中的变量名 命名规则，也就是c++的变量名

### 1.命名规则（and 和 or是关键字）
    1.只能包含字母，数字和下划线_
    2.不能以数字开头
    3.不能使用关键字
    4.严格区分大小写
### 2. 命名规范--可以不遵守，官方文档建议
     1.见名知意
     2.多个部分使用下划线连接---蛇形命名法
     3.英文字母全小写



## 常见数据类型

### 1.整数   int 

### 2.浮点数  float

### 3.字符串 str

### 4.布尔 bool

### 5.空值 NoneType

### 利用type()可以查找字面和变量的类型

### isinstance可以检查数据是否属于指定的类型，isinstance(数据,类型)




## 字符串的学习 -- 与c++不同
###  1.双引号定义
    s1 = "hello"
###  2.单引号定义
    s2 = 'pycharm'
    特殊 转义字符串 ---不是单引号的内容
    1: \'表示单引号
    2：\"表示双引号
    3: \n表示开新的一行
    4: \t 制表符 增加缩进，缩进一个制表符（tab）的大小
### 3.三引号定义(多行字符串)————c++没有
    s3 = """
       尊敬
       afc
       kk  """
### 单引号与双引号 --- 他俩不能直接换行
### 字符串的拼接   
   1.sss = "1" "2"
   2.sss = "1" + "2"
   3.print(sss + sss +"1")
### str(int ) 转换类型
### 字符串的格式化  
    1.print 时候传递数据  %s -- % s1 
    2.f"*****{}"---   不用指定,只需在{}中填写变量名即可

## 输入input
   有一个变量 来接受input的值，input()可以有提示信息，然后输入内容，很方便
   录入之后的内容都是字符串类型

# 运算符   浮点数的计算会有精度损失

## 1.算术运算符
   +加 -减  * % / --代表除法会有小数
   py独有 // --整除
   ** 幂指数 10 ** 3 -- 10的三次方
### 优先级 ** * / // % + -

## 2.赋值运算符（几乎无差别）
    = *= -= /= %= //= **=
## 3.比较运算符（无差别）
    == != > >= <  <=
## 4.逻辑运算符
   并且(与) 或者（或） 取反（非）
###  and or 