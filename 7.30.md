# 控制流程 ---也就是if elif  ---


## 条件判断  逻辑方面跟c++无差别，但是语法方面有差别
        if 条件 : 
            代码块 
        else:
            代码块
        elif:       
## 模式匹配（c++没有）--多用于基于某个变量的多个固定值进行分支判断。
   用一个清晰的模板去精准的匹配数据的结构和内容，匹配成功则执行响应的操作
   match  bianliang  :  
            case biangliang  :

   最后以case _:
          代码块作为结尾
