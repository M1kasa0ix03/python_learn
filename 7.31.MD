# 循环 -- while  for break continue  

## while之后可以加一个else：在结束之后执行一个操作---已知循环开始的条件和结束的条件

## for循环  也是可以加一个else：在结束之后进行一个操作 -与c++完全不同  ---知道要循环的次数
    是从一个数据集中取出一个元素，轮询操作
    for 元素 in 数据集 :
        代码块
    else：结束操作

## 特别 range语句 --生成制定规则的数据序列
    用法1：range(end) ->获取一个从0开始，到end结束的数据序列（不含end本身）。
    用法2：range(start,end) ->获取一个从start开始，到end结束的数字序列（不含end本身）。
    用法3：range(start,end,step) ->获取一个从start开始，到end结束的数字序列，step为步长（不含end本身）。

## 循环嵌套 不就是循环套个循环
    练习打印长方形
    练习打印99乘法表

## break continue 仅仅可以在循环系统中使用

## while True 可以开一个无限循环，只有break可以跳出

## continue中断本次循环，进行下一次循环

## 想生成一个随机数 要引入一个random模块
    import random
    rd_num = random.randint(a:1,b:100)  ---生成的随机数在1 到 100
