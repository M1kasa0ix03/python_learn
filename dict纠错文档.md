# dict 纠错文档

## 一、错误现象

在 `dict.py` 中运行以下代码时，出现语法错误（`SyntaxError`）：

```python
ars = { "kai": 29, "ode": 8, "saka": 7 }
print(ars)
print(ars)
print(f"哈夫茨的号码是{ars["kai"]}")
```

报错提示大意是：字符串没有正常结束 / 表达式后面的字符无效。

## 二、错误原因（原理）

问题出在最后一行 f-string（格式化字符串）：

```python
print(f"哈夫茨的号码是{ars["kai"]}")
```

1. 这一行的 f-string 外层使用的是**双引号** `"..."`。
2. 在花括号 `{}` 内部，取字典值时又用了一对**双引号** `ars["kai"]`。
3. 当 Python 解释器读取字符串时，遇到 `{ars[` 后面的第一个双引号 `"`，就认为整个字符串在这里**结束了**。
4. 于是后面的 `kai"]}` 变成了「字符串之外的孤零零的字符」，Python 无法解析，从而报语法错误。

简单说：**外层和内层使用了相同的引号，导致字符串被提前截断。**

### 图解

```
print(f"哈夫茨的号码是{ars["kai"]}")
             ↑        ↑
             |        |
         字符串开始   这个双引号被当成字符串的结束
```

## 三、解决方法

**方法一（推荐）**：把内层引号改成单引号，让内外层引号不同即可：

```python
print(f"哈夫茨的号码是{ars['kai']}")
```

**方法二**：外层改用单引号，内层用双引号：

```python
print(f'哈夫茨的号码是{ars["kai"]}')
```

**方法三**：先取出值放到变量里，再放进 f-string：

```python
num = ars["kai"]
print(f"哈夫茨的号码是{num}")
```

## 四、补充说明

- 如果你的 Python 版本是 **3.12 及以上**，这种「同引号嵌套」的写法其实是**允许的**，不会报错；但在较旧版本中会报语法错误。
- 无论哪个版本，**让内外层引号不同**（或用变量暂存）都是最通用、最稳妥、最清晰的写法。
- 这个规则不仅适用于 f-string，普通字符串拼接、字符串里包含引号时同样要注意引号的配对问题。

## 五、本次修改记录

| 原代码（错误） | 修改后（正确） |
| --- | --- |
| `print(f"哈夫茨的号码是{ars["kai"]}")` | `print(f"哈夫茨的号码是{ars['kai']}")` |

---

# 案例二：购物车系统查询时返回了错误商品数据

## 一、错误现象

在 `dict案例.py` 的购物车系统中：

1. 第一次按 `1` 添加了商品 `ps5`。
2. 第二次按 `4` 查询商品，输入了一个**错误**的商品名称。
3. 结果程序仍然返回了 `ps5` 的数据。

## 二、错误原因（原理）

问题出在 `case "4"` 查询分支的 `else` 部分：

```python
case "4":
    fname = input("请输入你要查查询的商品的名称")
    if fname not in shopcart :
        print("不存在，请再次输入")
    else:
        for fname in shopcart :          # ← 问题在这里
            good_info = shopcart[fname]
            print(f"商品价格:{good_info['price']},商品数量:{good_info['nums']}")
```

1. `for fname in shopcart` 会**遍历整个字典**，把 `fname` 一次次重新赋值为字典里的每一个键（键名），并打印对应的数据。
2. 于是这段代码的实际效果变成了「打印货架上**所有**商品」，而不是「打印用户查询的**那一个**商品」。
3. 因为当前货架上只有 `ps5` 一条商品，所以看起来就像「返回了 ps5 的数据」。

> 注意：真正「输入了不存在的名称」时，`if fname not in shopcart` 会先拦截并打印「不存在」。用户之所以能走到 `else` 分支，通常是因为输入的名称其实**存在于**字典中（例如大小写、空格等和预期不一致），从而触发了 `for` 循环把全部商品打印出来。

## 三、解决方法

把 `else` 里多余的 `for` 循环去掉，直接取出用户查询的那个商品即可：

```python
else:
    good_info = shopcart[fname]      # 直接取用户查询的那个商品
    print(f"商品价格:{good_info['price']},商品数量:{good_info['nums']}")
```

## 四、本次修改记录

| 原代码（错误） | 修改后（正确） |
| --- | --- |
| `for fname in shopcart :`<br>`    good_info = shopcart[fname]`<br>`    print(...)` | `good_info = shopcart[fname]`<br>`print(...)` |
