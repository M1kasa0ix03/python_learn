# 《元组案例2》纠错文档

> 面向从 C++ 转学 Python 的同学，重点讲清 C++ 思维和 Python 思维的差异。

---

## 一、原始代码的两个错误

### 错误 1：累加变量没有在循环内重置（C++ 同学最容易犯）

**原始代码：**

```python
su = 0                    # 最开始你写了 sum = 0，后来改成 su，但放在循环外
for s in sdu:
    su += s[2] + s[3] + s[4]   # 每次都累加，越加越大
    avg = su / 3
```

**问题：** 你想算**每个学生**自己的总分，但 `su` 在循环外只初始化一次，于是：

| 学生 | 理想总分 | 实际 su |
|------|---------|---------|
| 第1个 wangl | 255 | 255（对）|
| 第2个 li | 275 | 530（错，多加了上一个）|
| 第3个 shi | 245 | 775（错，越滚越大）|

**为什么 C++ 同学会犯：** 在 C++ 里，如果变量是 `for` 循环内声明的局部变量，每次进入循环都会重新构造；但你可能习惯把 `sum` 写在循环外。**Python 没有 `int su = 0;` 这种「声明即初始化」的语法**，`su = 0` 只执行一次，所以必须在循环体内部重新赋值，或者干脆用临时变量。

**正确写法：**

```python
for sid, name, chinese, math, english in sdu:
    total = chinese + math + english   # 每个学生单独算，不累加
    avg = total / 3
```

---

### 错误 2：用 `sum` 当变量名，遮蔽了内置函数 `sum()`

**原始代码：**

```python
sum = 0
for s in sdu:
    sum += s[2] + s[3] + s[4]
    ...

print(sum(cs) / len(cs))   # 想调用内置 sum()，结果报错
```

**报错信息：**

```
TypeError: 'int' object is not callable
```

**原因：**

- Python 的 `sum` 是一个**内置函数**（`sum([1,2,3])` → 6）。
- 你用 `sum = 0` 把它变成了一个**整数变量**，覆盖掉了函数。
- 后面再写 `sum(cs)`，Python 会把它当成「把整数 `sum` 当函数调用」，于是报「int 不可调用」。

**为什么 C++ 同学会犯：** C++ 里 `std::sum` 这类名字通常来自头文件、有命名空间保护，你起名叫 `sum` 不容易和标准库函数冲突。而 Python 的 `sum`、`list`、`max`、`min`、`input`、`id`、`str`、`dict`、`len` 都是**裸奔的内置名字**，一旦你 `sum = 0`，全局都被你污染了。

**记住这条规则：**

> Python 里永远不要用内置函数名当变量名：`sum` `list` `dict` `str` `max` `min` `len` `input` `print` `type` `id` `range` `set` `tuple` `abs` `round` ...

---

## 二、元组解包（Unpacking）写法

Python 里解包就像 C++ 的「结构化绑定」（C++17 的 `auto [a, b] = ...`），但更灵活。

### 1. for 循环里直接拆开

```python
for sid, name, chinese, math, english in sdu:
    ...
```

等价于原来的下标写法，但可读性高得多，不用记 `s[0]` 是学号还是 `s[1]` 是名字。

### 2. 用下划线 `_` 占位（表示"这个我不要"）

```python
cs = [chinese for _, _, chinese, _, _ in sdu]
```

- `_` 是 Python 惯例的「占位符」，表示这个字段忽略不用。
- 上面这句：拿每个元组里的第 3 个元素（语文分），放进列表 `cs`。

### 3. 对比：C++ 和 Python 的对照

| 需求 | C++ 风格 | Python 解包 |
|------|---------|------------|
| 取出所有字段 | `s[0], s[1], s[2]...` | `sid, name, chinese, math, english = s` |
| 只要其中一个字段 | `s[2]` | `*_, chinese, _, _ = s` 或用列表推导 `_` 占位 |
| 交换两个值 | `temp = a; a = b; b = temp` | `a, b = b, a` |

---

## 三、两套完整写法（效果完全相同）

### 方法一：下标写法（保留你原来的风格，只修正两处错误）

```python
# 学生成绩单系统
sdu = (
    ("s001","wangl",85,92,78),
    ("s002","li",92,88,95),
    ("s003","shi",78,85,82),
    ("s004","zeng",99,79,91),
    ("s005","zhou",95,96,89),
    ("s006","wangz",76,82,77),
    ("s007","hong",89,91,94),
    ("s008","xul",75,69,82),
    ("s009","xum",86,89,98),
    ("s010","xun",66,59,72)
)

# 1. 每个学生的总分与平均分（下标写法）
for s in sdu:
    total = s[2] + s[3] + s[4]   # 修正1：用 total，且每个学生单独算，不累加
    avg = total / 3
    print(f"{s[0]},{s[1]},{total},{avg:.1f}")

# 2. 各科最低分、最高分、平均分
cs = [s[2] for s in sdu]
ms = [s[3] for s in sdu]
es = [s[4] for s in sdu]

print(f"语文最低分：{min(cs)},最高分:{max(cs)},平均分：{sum(cs)/len(cs)}")
print(f"数学最低分：{min(ms)},最高分:{max(ms)},平均分：{sum(ms)/len(ms)}")
print(f"英语最低分：{min(es)},最高分:{max(es)},平均分：{sum(es)/len(es)}")

# 3. 查找平均分大于 90 的学生
print("优秀学生如下")
for s in sdu:
    avg = (s[2] + s[3] + s[4]) / 3
    if avg > 90:
        print(f"学号:{s[0]},学生：{s[1]},平均分:{avg:.1f}")
```

### 方法二：元组解包写法（Python 地道写法）

```python
# 学生成绩单系统
sdu = (
    ("s001", "wangl", 85, 92, 78),
    ("s002", "li", 92, 88, 95),
    ("s003", "shi", 78, 85, 82),
    ("s004", "zeng", 99, 79, 91),
    ("s005", "zhou", 95, 96, 89),
    ("s006", "wangz", 76, 82, 77),
    ("s007", "hong", 89, 91, 94),
    ("s008", "xul", 75, 69, 82),
    ("s009", "xum", 86, 89, 98),
    ("s010", "xun", 66, 59, 72),
)

# 1. 每个学生的总分与平均分（元组解包）
for sid, name, chinese, math, english in sdu:
    total = chinese + math + english          # 每个学生单独算，不累加
    avg = total / 3
    print(f"{sid},{name},{total},{avg:.1f}")

# 2. 各科最低分、最高分、平均分（用 _ 占位忽略不需要的字段）
cs = [chinese for _, _, chinese, _, _ in sdu]
ms = [math for _, _, _, math, _ in sdu]
es = [english for _, _, _, _, english in sdu]

print(f"语文最低分：{min(cs)},最高分:{max(cs)},平均分：{sum(cs)/len(cs)}")
print(f"数学最低分：{min(ms)},最高分:{max(ms)},平均分：{sum(ms)/len(ms)}")
print(f"英语最低分：{min(es)},最高分:{max(es)},平均分：{sum(es)/len(es)}")

# 3. 查找平均分大于 90 的学生
print("优秀学生如下")
for sid, name, chinese, math, english in sdu:
    avg = (chinese + math + english) / 3
    if avg > 90:
        print(f"学号:{sid},学生：{name},平均分:{avg:.1f}")
```

**两处修正对照：**

| 位置 | 原代码 | 修正后 |
|------|--------|--------|
| 循环累加 | `su` 写循环外，`su += ...` 越加越大 | 循环内 `total = s[2]+s[3]+s[4]`（或解包的 `chinese+math+english`） |
| 变量命名 | `sum = 0` 遮蔽内置函数 | 改用 `total` / `avg`，`sum()` 恢复可用 |

---

## 四、给 C++ 同学的 3 条核心提醒

1. **变量不要写在循环外累加**：Python 没有「每次进入循环自动初始化」这回事，累加要么放循环体内，要么循环前先 `= 0` 且明确知道自己在做「全局累计」。

2. **内置函数名绝对不能当变量名**：`sum`、`list`、`max`、`min`、`len` 这些是「全局共享」的，你一赋值就永久遮蔽它们，而且是静默的、到运行时才报错。

3. **多用解包、少用下标**：Python 下标写法 `s[0]`、`s[1]` 容易记混，解包 `sid, name, chinese, math, english = s` 一目了然，这也是 Python 的「地道」写法。
