# 数据存储容器————可以存储多份不同类型数据的容器
# list容器 ---vector类似但不同  序列
    1.可以存储不同类型的元素
    2.元素有序，可以重复，元素可以修改
    3.有索引和下标，下标从0开始
    4.可以从后开始，索引在最后一个是-1，依次绝对值增加
    5.del用来删除元素
## 1.列表的切片（字符串和元组也可以切片）
    是指对操作的数据截取其中一部分的操作，列表，字符串和元组都支持切片操作
    1.有开始索引和结束索引，还有步长。序列数据[开始索引:结束索引:步长] 
    2.包含开始索引的元素，不包含结束索引对应的元素
    3.步长默认为1
    4.索引可以使用正向和反向
    5.最后一个冒号可以省略，第一个不可以
## 2.列表的常见方法---也就是功能（增删查）
    1.append() --在列表尾部追加元素
    2.insert() --在指定索引之前，插入该元素
    3.remove() --移除列表中第一个匹配到的值--也就是删除用户指定的元素
    4.pop() --删除指定索引的严肃（如果没有指定，就删除最后一个）
    5.sort() --排序（只有元素类型相同，才可以排序）
    6.reverse() --反转列表元素
    7.sum(list)---求和
    8.len(list)---list长度(元素个数)
    9.if num in newlist: ---检查一个元素是否在列表中   not in---不在
    10.解包操作：将列表这一类容器解开成一个一个独立的元素
    11.组包操作：将多个值合并在一个序列
    12.两个列表合并：利用解包操作,将列表解包并且放入新的列表中，用逗号分隔开
    13.两个列表合并也可以直接使用+
    14.列表推导式：按照一定的规则快速生成一个列表的方法->语法格式1:[要插入的值  for i in 序列]   语法格式2:[要插入的值  for i in 序列 if 条件]
# str容器 ---字符串--字符的容器
    特点: 1.不可变性 -- 不支持直接指定索引位置进行修改--也就是不可修改
          2.有序性
          3.可迭代性  可遍历输出出来
## 切片：和list的语法相同
    特殊：步长可以为-1,从后往前截取
## 常用方法：--都不会对字符串进行任何的更改
    1.find() --在字符串中查找子串，返回第一次索引的地方，找不到返回-1
    2.count() --统计字符串中出现的次数
    3.upper() --将字符串中的所有字符转换为大写
    4.lower() --将字符串中的所有字母转换为小写
    5.split() --将字符串中按照分隔符分割成列表
    6.strip() --去除字符串两端的空白字符或指定字符
    7.replace() --将字符串中的指定子串替换为新的子串
    8.startswith() --检查字符串是否以指定子串开头，返回布尔值
    9.endswith() --检查字符串是否以指定子串结尾，返回布尔值
# tuple容器
    列表元素是可以重复，有序，可以修改的
    元组一旦定义之后，只可以查询，不可以修改
    特点：1.可以储存不同类型的元素
          2.元素可以重复，有序，不可以修改(可以支持索引访问，切片)
## 元组的基本操作
    1.元组用小括号定义
    2.count()查找元素在元组的出现次数
    3.index()查找元素在元组的第一次出现的下标索引
    4.剩下的都和之前的差不多
    5.注意点：定义单个元素的元组用 s1 = tuple(100)或者单个元素后面加个,
### 组包与解包
    1.组包：将多个值合并到一个容器中
    2.解包：将容器解开成独立的元素，并且赋值给多个变量。*可以收集多余的元素，并且生成一个列表
    3.基础解包要保证变量的数量和元组中的数量一致
# set容器
    特点：1.可以存储多个数据
        2.自动去重
        3.无序，可修改，不可以用索引来访问元素
        4.用{}来定义
        5.会自动给数据排序
        6.他的操作方法跟离散数学的集合对集合的方法相似
## 操作
    1.定义空集合 s1 = set()
    2.add()将元素添加到集合中
    3.remove()移除集合中指定的元素
    4.pop()随即删除集合中的元素并且返回该元素
    5.clear() 清空集合
    6.difference() 求取两个集合中的差异（包含在第一个集合但不包含在第二个集合中的元素）-代表差异
    7.union() 求取两个集合的并集 两个集合放前方后无差别  
    8.intersection() 求取两个集合中的交集 两个集合放前方后无差别  -- & 这个符号直接代替了这个语法 求多个集合的交集的话，直接 s1 & s2 & s3 & s4
# dict容器
    哈希表 存储键值对类型数据 一个键对应一个值 
    特点:1.键值对映射
         2.键不能重复(重复之后后面的值会覆盖前面的值) 但可以修改
         3.key必须得是不可变类型(str,int,float,tuple)
## 操作
    1.定义字典 用{}   key : value
    2.字典名称  = dict()
    3.值 = 字典名称[key]
    4.添加 ars[key] = value
    5.删除-1.字典名称.pop(key)
           2.del 字典名称[key]
    6.修改 同4
    7.查询 1-字典名称[key]
           2-字典名称.get(key)
           3-字典名称.keys() -- 获取所有的key
           4-字典名称.values() --获取所有的value
           5-字典名称.items() 获取所有的key--value 键值对 拿到的为元组