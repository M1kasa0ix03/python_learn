# 教务管理系统纠错说明

## 1. 修复列表下标错误

原代码使用：

```python
self.student_list[name]
```

`student_list` 是列表，不能使用学生姓名作为下标。

修复方式：增加 `find_student()` 方法，根据姓名查找学生对象。

## 2. 修复循环提前结束问题

原代码在循环第一次匹配失败时就执行 `return`，导致后面的学生无法继续查找。

现在统一使用 `find_student()`，遍历完整个列表后再判断是否存在。

## 3. 修复删除功能

原来的删除功能只是将成绩改成 0，并没有删除学生。

现在使用：

```python
self.student_list.remove(student)
```

实现真正删除学生。

## 4. 修复成绩修改功能

现在通过学生对象调用：

```python
student.update_score(*scores)
```

修改成绩，不再错误地使用姓名访问列表。

## 5. 增加输入异常处理

如果输入非数字成绩，程序会提示：

```text
成绩必须输入数字
```

不会直接崩溃。

## 6. 增加空列表提示

没有学生时查看全部成绩，会提示：

```text
当前没有学生
```